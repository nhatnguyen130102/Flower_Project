@model IEnumerable<Flower_Models.BillDetails>

@{
    ViewData["Title"] = "Index";
    Layout ="~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Bill Details</h1>

<button>
    
</button>
<div class="tbl-container">
    <table>
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ID_Bill)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ID_Product)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Product_Quantity)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Total)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.ID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ID_Bill)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ID_Product)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Product_Quantity)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Total)
                        </td>
                        @*<td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                        </td>*@
                        <td>
                            <a href="@Url.Action("Edit", "BillDetails", new { id = item.ID })">
                                <i class="fa-solid fa-pen-to-square"></i>
                            </a>
                            <a href="@Url.Action("Details", "BillDetails", new { id = item.ID })">
                                <i class="fa-solid fa-circle-info"></i>
                            </a>
                            <a href="@Url.Action("Delete", "BillDetails", new { id = item.ID })">
                                <i class="fa-solid fa-trash-can"></i>
                            </a>
                        </td>
                    </tr>
                }
        </tbody>
    </table>
</div>
