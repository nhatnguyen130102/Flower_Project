@model IEnumerable<Flower_Models.CustomerType>

@{
    ViewData["Title"] = "Index";
    Layout ="~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Customer Type</h1>

<div class="tbl-container">
    <table>
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ID_CustomerType)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name_CustomerType)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MinSpend)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MaxSpend)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Discount)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.ID_CustomerType)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name_CustomerType)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MinSpend)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MaxSpend)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Discount)
                        </td>
                       @* <td>
                            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                        </td>*@

                        <td>
                            <a href="@Url.Action("Edit", "CustomerType", new { id = item.ID_CustomerType })">
                                <i class="fa-solid fa-pen-to-square"></i>
                            </a>

                            <a href="@Url.Action("Details", "CustomerType", new { id = item.ID_CustomerType })">
                                <i class="fa-solid fa-circle-info"></i>
                            </a>

                            <a href="@Url.Action("Delete", "CustomerType", new { id = item.ID_CustomerType })">
                                <i class="fa-solid fa-trash-can"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
    </table>
</div>
