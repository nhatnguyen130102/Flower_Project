@model IEnumerable<Flower_Models.FlashSale>

@{
    ViewData["Title"] = "Index";
    Layout ="~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>FlashSale</h1>

<div class="tbl-container"><table>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_FlashSale)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price_FlashSale)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID_FlashSale)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price_FlashSale)
                    </td>
                   @* <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>*@
                    <td>
                        <a href="@Url.Action("Edit", "FlashSale", new { id = item.ID_FlashSale })">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>

                        <a href="@Url.Action("Details", "FlashSale", new { id = item.ID_FlashSale })">
                            <i class="fa-solid fa-circle-info"></i>
                        </a>

                        <a href="@Url.Action("Delete", "FlashSale", new { id = item.ID_FlashSale })">
                            <i class="fa-solid fa-trash-can"></i>
                        </a>
                    </td>
                </tr>
        }
    </tbody>
</table></div>
