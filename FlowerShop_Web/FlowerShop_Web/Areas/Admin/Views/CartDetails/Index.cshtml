@model IEnumerable<Flower_Models.CartDetails>

@{
    ViewData["Title"] = "Index";
    Layout ="~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Cart Details</h1>

<button>
    
</button>
@*<div class="overflow-auto p-4 shadow-lg h-96 rounded-xl">*@
<div class="tbl-container">
    <table>
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ID_Cart)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ID_Product)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Product_Quantity)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID_Cart)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID_Product)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Product_Quantity)
                    </td>
                    @*<td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>*@
                    @*<td>
                        <a href="@Url.Action("Edit", "CartDetails", new { id = item.ID })">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>
                        <a href="@Url.Action("Details", "CartDetails", new { id = item.ID })">
                            <i class="fa-solid fa-circle-info"></i>
                        </a>
                        <a href="@Url.Action("Delete", "CartDetails", new { id = item.ID })">
                            <i class="fa-solid fa-trash-can"></i>
                        </a>
                    </td>*@
                </tr>
        }
        </tbody>
    </table>
</div>
