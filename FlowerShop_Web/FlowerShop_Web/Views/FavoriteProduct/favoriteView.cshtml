@model IEnumerable<Flower_Models.FavoriteProductDetails>

@{
    ViewData["Title"] = "favoriteView";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>favoriteView</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_FavoriteProduct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model is not null)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID_FavoriteProduct)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID_Product)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        }
        else
        {
            
        }
    </tbody>
</table>
